{
  "name": "cav-chef",
  "version": "0.1.0",
  "description": "CAV Slackbot — reads a Slack List of inventory, flags items below threshold, and posts Approve/Deny reorder prompts to Slack.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node --test",
    "check-inventory-list": "node scripts/check-inventory-list.js",
    "check-calendar": "node scripts/check-calendar.js",
    "run-reorder-cycle": "node scripts/run-reorder-cycle.js",
    "run-checkin-poll": "node scripts/run-checkin-poll.js",
    "audit-log": "node scripts/audit-log.js",
    "lint": "eslint ."
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@slack/bolt": "4.7.3",
    "@slack/web-api": "^8.0.0",
    "dotenv": "~17.4.2",
    "googleapis": "^173.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0"
  }
}
